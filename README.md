# Grafana plugin playground

- `docker-compose up`
- attach a shell to the grafana container
  - `grafana-cli plugins install agenty-flowcharting-panel`
- restart docker container
- et voila!

## Kibana: Dev tools:

http://localhost:5601/app/dev_tools#/console